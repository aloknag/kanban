## Document containing e2e test cases

> **Design philosophy:** AgentBoard is a "field journal" — columns read top-to-bottom as a single vertical document. Visual bugs that break vertical rhythm, baseline alignment, or document-like reading flow are high severity.
>
> **QA visual review standard:** When reviewing screenshots, ask: *How would a real user feel seeing this?* Would they be confused? Do lines, colors, spacing, and groupings communicate clearly? Flag anything that looks off — misaligned rules, unexpected colors, broken rhythm, ambiguous affordances — even if it's not a functional failure.

| Test Case |Test Dimension | Status | Date Executed | GitHub Tracker if test failed |
|-----------|---------------|--------|---------------|-------------------------------|
| When: Human user creates a new task with a name of 600 characters. And: Fills in the task body with a normal-length string. Then: The task is created successfully, and the name displays fully and correctly on the board. | Boundary values | FAIL | 2026-05-03 (re-validated 2026-05-03) | https://github.com/aloknag/testfiles/issues/36 |
| When: Human user creates a task in "To Do" column. And: Moves the task to "In Progress", then to "Done", then back to "To Do". Then: The task status updates correctly at each step and persists after page refresh. | State transitions | PASS | 2026-05-03 | - |
| When: Agent creates a task via API with special characters in title (e.g. `<script>alert(1)</script>` and unicode emoji 🚀). And: Human opens the board. Then: The task title renders safely (no XSS execution) and the unicode displays correctly. | UI rendering / Boundary values | PASS | 2026-05-03 | - |
| When: Agent sends a PATCH request to move a task to a non-existent column_id (e.g. 9999). Then: The API returns an appropriate error (4xx) and the task remains in its original column. | API contract / Error states | FAIL | 2026-05-03 | https://github.com/aloknag/testfiles/issues/37 |
| When: Agent creates a new task via API (POST /api/tasks) with a known title. And: Human opens the board in the browser. Then: The task appears on the board under the correct column without requiring any manual interaction. | Cross-persona | PASS | 2026-05-03 | - |
| When: Human opens the board. And: The API returns 4 columns including a duplicate "Todo" column (id=4, position=3). Then: The board should only show 3 canonical columns (Todo, In Progress, Done) with no duplicates. | Data integrity | FAIL | 2026-05-03 | https://github.com/aloknag/testfiles/issues/38 |
| When: Two browser tabs have the board open simultaneously. And: Tab A moves Task X to "In Progress" while Tab B moves the same Task X to "Done" at the same time. Then: After both operations settle, Task X appears in exactly one column on both tabs with no ghost/duplicate card. | Concurrency | PASS | 2026-05-03 | - |
| When: Agent creates a task whose body contains a fenced code block (```python) and a Mermaid diagram (```mermaid). And: Human opens the task detail view. Then: The code block renders with syntax highlighting and the Mermaid diagram renders as a diagram (not raw text). | UI rendering | PASS | 2026-05-03 | - |
| When: Human attempts to create a new task via the UI with an empty (blank) title. Then: The UI prevents submission and shows a validation error — no empty-title task is created. | Error states | FAIL | 2026-05-03 | https://github.com/aloknag/testfiles/issues/39 |
| When: Agent bulk-creates 100 tasks via API in the Todo column. And: Human opens the board in the browser. Then: The board renders all tasks within 3 seconds with no JS errors and the correct task count is shown. | Performance | PASS | 2026-05-03 | - |
| When: Agent creates an epic via API, then links a task to that epic via PATCH. And: Human opens the task detail view. Then: The epic name is displayed on the task and the task appears under the epic on the Epics page. | Happy path (Epics) | FAIL | 2026-05-03 | https://github.com/aloknag/testfiles/issues/40 |
| When: Agent restarts the backend container (docker compose restart backend). And: Human opens the board in the browser. Then: All previously created tasks and their metadata (title, slug, column, epic link) are still present. | Data persistence | PASS | 2026-05-03 | - |
| When: Agent deletes a task via DELETE /api/tasks/{id} (expects HTTP 204). And: Human navigates directly to /tasks/{id} in the browser. Then: The API returns HTTP 404 for the missing task and the UI shows a "not found" error. | Error states / 404 handling | FAIL | 2026-05-03 | https://github.com/aloknag/testfiles/issues/41 |
| When: Agent creates a task via POST /api/tasks with assignee set to "alice". And: Human opens the task detail view in the browser. Then: The assignee "alice" is displayed on the task detail page. | Cross-persona / Assignee field | PASS | 2026-05-03 | - |
| When: Human opens the task detail page for a task assigned to "alice". And: Agent updates the task's assignee to "bob" via PATCH /api/tasks/{id}. Then: The browser shows "bob" as the assignee without requiring a manual page refresh. | Live update / Polling | PASS | 2026-05-03 | - |
| When: Agent sends PATCH /api/tasks/{id} with title set to "" (empty string). Then: The API returns a 4xx error and the task retains its original non-empty title. | API contract / Input validation (PATCH) | FAIL | 2026-05-03 | https://github.com/aloknag/testfiles/issues/42 |
| When: Agent creates an epic with 3 linked tasks via API. And: Human opens the Epics page. Then: The epic is listed with the correct task count and progress (e.g. 1/3 done). And: Clicking the epic shows all 3 linked tasks by title. | Happy path (Epic detail view) | FAIL | 2026-05-03 | https://github.com/aloknag/testfiles/issues/43 |
| When: Agent creates a task with a unique title "CatalogSearch-UniqueXYZ". And: Human opens the board and uses the CATALOG sidebar to search for "UniqueXYZ". Then: Only the matching task is shown and clicking it navigates to its detail page. | Search / Filtering (Catalog sidebar) | FAIL | 2026-05-04 | https://github.com/aloknag/testfiles/issues/44 |
| When: Agent creates a new column "QA-Test-Column" via POST /api/columns, then creates a task assigned to that column. And: Human opens the board. Then: The new column appears with the task card inside it. | Cross-persona / Column lifecycle | FAIL | 2026-05-06 | https://github.com/aloknag/testfiles/issues/48 |
| When: Human opens the board and presses Tab to navigate through task cards. And: Presses Enter on a focused task card. Then: Focus moves through interactive elements in logical order and Enter on a focused task card navigates to its detail page. | Keyboard / Accessibility | FAIL | 2026-05-06 | https://github.com/aloknag/testfiles/issues/49 |
| When: Human opens the board. Then: The Done column is visible with its task cards shown (or clearly signals it is collapsed with a reachable toggle). | UI rendering / Done column visibility | FAIL | 2026-05-06 | https://github.com/aloknag/testfiles/issues/50 |
| When: Human opens the board with columns containing different numbers of cards. Then: The horizontal divider lines at the bottom of each column's card list are visually aligned at a consistent baseline across all columns. | UI rendering / Visual alignment | FAIL | 2026-05-06 | https://github.com/aloknag/testfiles/issues/51 |
| When: Human creates a new task via the UI. And: Presses Ctrl+Z or uses browser's back button. Then: The task creation is undone and the task disappears from the board. | Undo/Redo functionality | INVALID | 2026-05-09 | - | Test premise incorrect: AgentBoard intentionally has no task creation UI for humans (FrontEngDesign §3.1: "no + New button — creation happens via API"). This scenario is out of scope by design. |
| When: Agent creates 2 epics ("Data Pipeline", "Dashboard UI") with 3 tasks each via API, moves Pipeline tasks to Done and Dashboard tasks to In Progress, and posts journal entries marking the dependency unblocked. And: Human opens the Epics page. Then: Each epic shows the correct done/total count (3/3 and 0/3 respectively), and the Board shows tasks distributed correctly across columns. | Agent workflow simulation / Multi-epic lifecycle | PASS | 2026-05-09 | - |
| When: Agent creates a task via API with initial markdown content (v1). And: Human opens the task detail page. And: Agent edits the .md file directly on disk (no API call). Then: Within 5 seconds the task detail page automatically shows the updated content (v2) without a manual refresh — validating TDD §2's edit-in-place polling guarantee. | Agent edit-in-place / Polling fidelity | PASS | 2026-05-09 | - |
| When: Agent calls GET /api/tasks?column_id=1, then ?column_id=2, then ?column_id=3. Then: Each response contains only tasks in that column. (Discovered while setting up epic re-linking test.) | API contract / column_id query filter | FAIL | 2026-05-09 | https://github.com/aloknag/testfiles/issues/53 |
| When: Human navigates directly to /tasks/abc or /epics/abc (non-numeric ID) in the browser. Then: The page shows a "not found" state, consistent with numeric-but-nonexistent IDs (e.g. /tasks/-1, which correctly shows "not found"). | Error states / URL param handling | FAIL | 2026-07-25 | https://github.com/aloknag/testfiles/issues/54 |
| When: Agent calls GET /api/columns. Then: Each column's task_count field equals the actual number of tasks with that column_id (cross-checked against GET /api/tasks). | API contract / Data integrity | FAIL | 2026-07-25 | https://github.com/aloknag/testfiles/issues/55 |
| When: Agent creates a column, creates a task inside it, then calls DELETE /api/columns/{id} while the task still references it. Then: The API returns a 4xx error (e.g. 409 Conflict), not a 500 crash. | API contract / Error states | FAIL | 2026-07-25 | https://github.com/aloknag/testfiles/issues/56 |
| When: Agent calls DELETE /api/epics/{id} on an epic that still has linked tasks (e.g. EPIC-001). Then: The API returns a 4xx error, not a 500 crash. | API contract / Error states | FAIL | 2026-07-25 | https://github.com/aloknag/testfiles/issues/57 |
| When: Agent sends PATCH /api/tasks/{id} with epic_id set to a non-existent epic (e.g. 999999). Then: The API returns a 4xx validation error, not a 500 crash. | API contract / Input validation | FAIL | 2026-07-25 | https://github.com/aloknag/testfiles/issues/58 |
| When: Agent sends an invalid POST /api/tasks (missing content_path) which correctly fails with 400. And: Agent immediately sends a second, unrelated but similarly-shaped POST /api/tasks with a valid content_path. Then: The second request succeeds or fails cleanly — it does not crash with a spurious 500 "slug_collision" for a title that has no real conflict. | API contract / Data integrity | FAIL | 2026-07-25 | https://github.com/aloknag/testfiles/issues/59 |
| When: Agent calls GET /api/tasks/{id} with an ID exceeding normal integer range (26 digits). Then: The API returns 404 Not Found (consistent with other nonexistent numeric IDs, e.g. /api/tasks/-5), not a 500 crash. | API contract / Boundary values | FAIL | 2026-07-25 | https://github.com/aloknag/testfiles/issues/60 |
| When: Agent calls PATCH /api/columns/{id} with name set to "" (empty string). Then: The API rejects the request with a 4xx validation error, or at minimum does not persist an empty column name. | API contract / Input validation | FAIL | 2026-07-25 | https://github.com/aloknag/testfiles/issues/61 |
| When: Human types text into the journal "who are you?" / "add journal entry" inputs. Then: The typed text is investigated for low readability/contrast against the placeholder styling. | UI rendering / Accessibility | PASS (false positive — ruled out) | 2026-07-25 | - |
| When: Human performs a real mouse drag-and-drop of a task card from one board column to another. Then: The task's column updates and a PATCH request is observed. | State transitions / Drag-and-drop | INCONCLUSIVE (automation limitation, not confirmed as app defect) | 2026-07-25 | - |
